<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>LoggerNotificationRule__c</fullName>
    <deleteConstraint>SetNull</deleteConstraint>
    <inlineHelpText>Denormalized lookup to the notification rule that produced this delivery. Duplicates the LoggerNotificationRule__c on the parent match row so reports and related lists can filter deliveries by rule without a subquery hop through match.</inlineHelpText>
    <label>Logger Notification</label>
    <referenceTo>LoggerNotificationRule__c</referenceTo>
    <relationshipLabel>Logger Notification Deliveries</relationshipLabel>
    <relationshipName>LoggerNotificationDeliveries</relationshipName>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Lookup</type>
</CustomField>
