<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>User__c</fullName>
    <deleteConstraint>SetNull</deleteConstraint>
    <inlineHelpText>Salesforce User to notify. Interpretation is up to the service's Notifier Apex class (e.g. an Email service may resolve this to the user's Email; a Slack service may resolve to their Slack ID).</inlineHelpText>
    <label>User</label>
    <referenceTo>User</referenceTo>
    <relationshipLabel>Logger Notification Recipients</relationshipLabel>
    <relationshipName>LoggerNotificationRecipients</relationshipName>
    <required>false</required>
    <trackHistory>true</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Lookup</type>
</CustomField>
